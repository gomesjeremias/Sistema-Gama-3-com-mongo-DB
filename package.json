{
  "name": "sales-app-mongodb",
  "version": "1.0.0",
  "description": "Sistema de Vendas com MongoDB e Prisma",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "dev2": "node --watch server_fixed.js",
    "dev": "npx http-server -p 8000",
    "start": "npx http-server -p 8000",
    "db:generate": "npx prisma generate",
    "db:studio": "npx prisma studio",
    "db:reset": "npx prisma db push --force-reset",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sales",
    "mongodb",
    "prisma",
    "javascript",
    "frontend"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "prisma": "^6.12.0"
  }
}
